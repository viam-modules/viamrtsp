name: Test build viamrtsp for Android

on: [push, pull_request]

jobs:
  build-android:
    uses: ./.github/workflows/build-module.yml
    with:
      target_os: android
      target_arch: arm64
      docker_image: ghcr.io/viamrobotics/antique2:amd64-cache
